# Zodiac Crypto Prediction Backend 🔮💰

Бекенд на Hono з AI endpoint для створення жартівливих крипто-передбачень на основі знаку зодіаку та аналізу криптовалютного портфоліо користувача.

## Налаштування

### 1. Встановіть залежності

```bash
npm install
```

### 2. Налаштуйте змінні середовища

Створіть файл `.env` у кореневій директорії:

```bash
cp .env.example .env
```

Відредагуйте `.env` та додайте необхідні API ключі:

```
OPENAI_API_KEY=sk-your-actual-api-key-here
OCTAV_API_KEY=your-octav-api-key-here
PORT=3000
```

**Де отримати API ключі:**
- **OpenAI API Key**: https://platform.openai.com/api-keys
- **Octav API Key**: https://data.octav.fi/ (потрібен для аналізу портфоліо та транзакцій)

**Примітка:** Для астрологічних даних використовується безкоштовний Aztro API (https://github.com/sameerkumar18/aztro), який не потребує API ключа.

### 3. Запустіть сервер

**Режим розробки (з автоперезавантаженням):**
```bash
npm run dev
```

**Production режим:**
```bash
npm run build
npm start
```

## API Endpoints

### 🔮 POST /api/zodiac-prediction

**Головний endpoint для отримання крипто-передбачень на основі знаку зодіаку та портфоліо.**

Endpoint підтримує **режим діалогу** - AI сам попросить необхідну інформацію, якщо її не вистачає. Можна надіслати запит навіть без даних, і AI поведе розмову для збору інформації.

**Request з усіма даними:**
```json
{
  "message": "Створи для мене передбачення",
  "birthDate": "1990-05-15",
  "walletAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb"
}
```

**Request без даних (AI попросить інформацію):**
```json
{
  "message": "Привіт! Хочу передбачення"
}
```

**Request з історією розмови:**
```json
{
  "message": "Лев",
  "conversationHistory": [
    {
      "role": "user",
      "content": "Хочу передбачення"
    },
    {
      "role": "assistant",
      "content": "Привіт! 👋 Щоб створити твоє персональне крипто-передбачення, мені потрібно знати твій знак зодіаку..."
    }
  ]
}
```

**Параметри:**
- `message` (optional): Повідомлення користувача
- `birthDate` (optional): Дата народження в форматі ISO (YYYY-MM-DD)
- `zodiacSign` (optional): Знак зодіаку (українською або англійською)
- `walletAddress` (optional): Адреса Ethereum гаманця
- `conversationHistory` (optional): Історія попередніх повідомлень

**Response з повною інформацією:**
```json
{
  "success": true,
  "message": "🐂 О, Тельцю! Бачу, ти справжній HODLER з diamond hands 💎...",
  "zodiac": {
    "sign": "Телець",
    "emoji": "♉",
    "dates": "20.04 - 20.05",
    "element": "Земля",
    "traits": ["стабільний", "терплячий", "любить HODL", "не любить волатильність"]
  },
  "wallet": {
    "address": "0x742d35...",
    "networth": "1504.09",
    "totalAssets": 3,
    "topAssets": [
      {
        "symbol": "UNI",
        "name": "uniswap",
        "balance": "69.9031",
        "value": "1011.49"
      }
    ]
  },
  "usage": {
    "prompt_tokens": 450,
    "completion_tokens": 200,
    "total_tokens": 650
  }
}
```

**Response коли AI просить інформацію:**
```json
{
  "success": true,
  "message": "Привіт! 👋 Щоб створити твоє персональне крипто-передбачення, мені потрібно знати твій знак зодіаку або дату народження. Який у тебе знак зодіаку? 🌟",
  "usage": {
    "prompt_tokens": 120,
    "completion_tokens": 50,
    "total_tokens": 170
  }
}
```

### 📋 GET /api/zodiac-signs

Отримує список всіх знаків зодіаку з детальною інформацією.

**Response:**
```json
{
  "signs": [
    {
      "key": "aries",
      "sign": "Овен",
      "emoji": "♈",
      "dates": "21.03 - 19.04",
      "element": "Вогонь",
      "traits": ["імпульсивний", "сміливий", "енергійний", "FOMO-схильний"]
    },
    // ... інші знаки
  ]
}
```

### POST /api/chat

Відправляє промпт до AI та отримує відповідь. Підтримує історію розмови.

**Request:**
```json
{
  "prompt": "Розкажи про створення API на Hono",
  "conversationHistory": [
    {
      "role": "user",
      "content": "Привіт!"
    },
    {
      "role": "assistant",
      "content": "Привіт! Чим можу допомогти?"
    }
  ]
}
```

**Response:**
```json
{
  "success": true,
  "prompt": "Розкажи про створення API на Hono",
  "response": "Відповідь від AI...",
  "model": "gpt-3.5-turbo",
  "usage": {
    "prompt_tokens": 15,
    "completion_tokens": 150,
    "total_tokens": 165
  }
}
```

### POST /api/config/system-prompt

Оновлює початкові знання AI (system prompt).

**Request:**
```json
{
  "systemPrompt": "Ти експерт з TypeScript та Hono framework."
}
```

**Response:**
```json
{
  "success": true,
  "message": "System prompt updated",
  "systemPrompt": "Ти експерт з TypeScript та Hono framework."
}
```

### GET /api/config/system-prompt

Отримує поточний system prompt AI.

**Response:**
```json
{
  "systemPrompt": "Ти - корисний AI асистент..."
}
```

### GET /

Health check endpoint.

**Response:**
```json
{
  "status": "ok",
  "message": "Zodiac AI Backend is running"
}
```

## Приклади використання

### Отримання крипто-передбачення

#### Варіант 1: З усіма даними одразу

```bash
curl -X POST http://localhost:3000/api/zodiac-prediction \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Створи для мене передбачення",
    "birthDate": "1995-08-23",
    "walletAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb"
  }'
```

#### Варіант 2: Діалоговий режим (AI просить інформацію)

**Крок 1: Початковий запит без даних**
```bash
curl -X POST http://localhost:3000/api/zodiac-prediction \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Привіт! Хочу дізнатись своє крипто-передбачення"
  }'
```

**Відповідь AI:**
```json
{
  "success": true,
  "message": "Привіт! 👋 Щоб створити твоє персональне крипто-передбачення, мені потрібно знати твій знак зодіаку або дату народження. Який у тебе знак зодіаку? 🌟"
}
```

**Крок 2: Надання знаку зодіаку**
```bash
curl -X POST http://localhost:3000/api/zodiac-prediction \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Я Лев",
    "zodiacSign": "лев",
    "conversationHistory": [
      {
        "role": "user",
        "content": "Хочу дізнатись своє крипто-передбачення"
      },
      {
        "role": "assistant",
        "content": "Привіт! 👋 Щоб створити твоє персональне крипто-передбачення..."
      }
    ]
  }'
```

**Відповідь AI:**
```json
{
  "success": true,
  "message": "Супер! Тепер мені потрібна адреса твого крипто-гаманця (Ethereum), щоб проаналізувати портфоліо і створити точне передбачення. Надішли адресу у форматі 0x... 💼"
}
```

**Крок 3: Надання адреси гаманця**
```bash
curl -X POST http://localhost:3000/api/zodiac-prediction \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Ось моя адреса",
    "zodiacSign": "лев",
    "walletAddress": "0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb",
    "conversationHistory": [...]
  }'
```

#### JavaScript/Fetch приклад

```javascript
// Початковий запит
let conversationHistory = [];

const response1 = await fetch('http://localhost:3000/api/zodiac-prediction', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    message: 'Хочу передбачення!'
  })
});

const data1 = await response1.json();
console.log('AI:', data1.message);

// Додаємо до історії
conversationHistory.push(
  { role: 'user', content: 'Хочу передбачення!' },
  { role: 'assistant', content: data1.message }
);

// Відповідаємо зі знаком зодіаку
const response2 = await fetch('http://localhost:3000/api/zodiac-prediction', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    message: 'Я Стрілець',
    zodiacSign: 'стрілець',
    conversationHistory
  })
});

const data2 = await response2.json();
console.log('AI:', data2.message);
```

### Використання AI чату

#### cURL

```bash
curl -X POST http://localhost:3000/api/chat \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Привіт! Як справи?"}'
```

#### JavaScript/Fetch

```javascript
const response = await fetch('http://localhost:3000/api/chat', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
  },
  body: JSON.stringify({
    prompt: 'Привіт! Як справи?'
  })
});

const data = await response.json();
console.log(data.response);
```

## AI Модуль

Логіка роботи з AI винесена в окремий модуль `src/services/ai.ts`. Це дозволяє:

- ✅ Легко модифікувати поведінку AI
- ✅ Додавати початкові знання (system prompt)
- ✅ Керувати історією розмов
- ✅ Змінювати налаштування моделі в одному місці

### Налаштування AI

В `src/index.ts` можна змінити початкові знання AI:

```typescript
const aiService = new AIService({
  apiKey: process.env.OPENAI_API_KEY || '',
  model: 'gpt-3.5-turbo',
  temperature: 0.7,
  maxTokens: 1000,
  systemPrompt: `Тут твої власні початкові знання для AI`,
});
```

### Методи AIService

- `chat(request)` - головний метод для чату з підтримкою історії
- `simpleChat(prompt)` - простий запит без історії
- `setSystemPrompt(prompt)` - оновити початкові знання
- `getSystemPrompt()` - отримати поточні початкові знання
- `setModel(model)` - змінити модель AI
- `setTemperature(temp)` - змінити креативність відповідей

## Технології

- **Hono** - швидкий веб-фреймворк
- **OpenAI API** - генерація AI-передбачень
- **Octav API** - аналіз крипто портфоліо та транзакцій
- **TypeScript** - типізація
- **tsx** - швидкий запуск TypeScript

## Структура проєкту

```
zodiac/
├── src/
│   ├── services/
│   │   ├── ai.ts         # AI модуль (OpenAI логіка)
│   │   └── octav.ts      # Octav API інтеграція
│   ├── utils/
│   │   └── zodiac.ts     # Утиліти для знаків зодіаку
│   └── index.ts          # Головний файл сервера з endpoints
├── .env                   # Змінні середовища (не в git)
├── .env.example          # Приклад змінних середовища
├── package.json          # Залежності та скрипти
├── tsconfig.json         # Конфігурація TypeScript
└── README.md             # Документація
```

## Особливості

✨ **Крипто-Астрологія**: Поєднання знаків зодіаку з аналізом крипто-портфоліо
💬 **Діалоговий Режим**: AI сам попросить необхідну інформацію - знак зодіаку, дату народження або адресу гаманця
📊 **Детальний Аналіз**: Портфоліо, історія транзакцій, витрати на комісії
🤖 **AI-Powered**: Генерація унікальних жартівливих передбачень
💎 **Crypto Jargon**: Використання популярних крипто-термінів та мемів
🌐 **Multi-Chain**: Підтримка всіх блокчейнів через Octav API
🔐 **Безпечно**: Тільки читання даних, без доступу до приватних ключів
🗣️ **Conversation History**: Підтримка контексту розмови для природного діалогу
